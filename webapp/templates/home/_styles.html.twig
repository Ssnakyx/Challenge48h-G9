<style>
    :root {
        --teal-950: #011a17;
        --teal-900: #052e28;
        --teal-800: #0a4a3e;
        --teal-600: #0d6b5a;
        --teal-400: #1ab394;
        --teal-200: #7de2cf;
        --teal-100: #c0f3ea;
        --amber-400: #fbbf24;
        --rose-500: #f43f5e;
        --slate-900: #0f172a;
        --slate-800: #1e293b;
        --slate-700: #334155;
        --glass-bg: rgba(255,255,255,0.04);
        --glass-border: rgba(255,255,255,0.08);
    }

    * { box-sizing: border-box; }

    html, body {
        margin: 0; padding: 0;
        background: #011a17 !important;
        color: #e2f8f4;
        font-family: 'Cabinet Grotesk', sans-serif;
        overflow-x: hidden;
    }

    /* ── Ambient background ── */
    #ambient-bg {
        position: fixed;
        inset: 0;
        z-index: 0;
        overflow: hidden;
        pointer-events: none;
    }
    .mesh-blob {
        position: absolute;
        border-radius: 50%;
        filter: blur(100px);
        opacity: 0.3;
        animation: blobDrift 18s ease-in-out infinite;
    }
    .mesh-blob:nth-child(1) { width: 700px; height: 700px; background: radial-gradient(circle, #1ab394, #052e28); top: -200px; left: -200px; animation-delay: 0s; }
    .mesh-blob:nth-child(2) { width: 500px; height: 500px; background: radial-gradient(circle, #0d6b5a, #011a17); bottom: -100px; right: -100px; animation-delay: -6s; }
    .mesh-blob:nth-child(3) { width: 350px; height: 350px; background: radial-gradient(circle, #7de2cf, #0a4a3e); top: 40%; left: 40%; animation-delay: -12s; }

    @keyframes blobDrift {
        0%, 100% { transform: translate(0, 0) scale(1); }
        33% { transform: translate(40px, -30px) scale(1.05); }
        66% { transform: translate(-20px, 50px) scale(0.95); }
    }

    /* ── Aurora waves ── */
    .aurora-container {
        position: fixed;
        inset: 0;
        z-index: 0;
        pointer-events: none;
        overflow: hidden;
        opacity: 0.22;
    }
    .aurora-wave {
        position: absolute;
        width: 200%;
        height: 300px;
        left: -50%;
        background: linear-gradient(180deg, transparent 0%, rgba(26,179,148,0.3) 40%, rgba(13,107,90,0.15) 70%, transparent 100%);
        border-radius: 50%;
        filter: blur(40px);
        animation: auroraShift 12s ease-in-out infinite;
    }
    .aurora-wave:nth-child(1) { top: -80px; animation-duration: 12s; animation-delay: 0s; }
    .aurora-wave:nth-child(2) { top: -40px; animation-duration: 16s; animation-delay: -4s; opacity: 0.8; }
    .aurora-wave:nth-child(3) { top: 0px; animation-duration: 20s; animation-delay: -8s; opacity: 0.5; }

    @keyframes auroraShift {
        0%, 100% { transform: translateX(0) scaleY(1) rotate(0deg); }
        25% { transform: translateX(5%) scaleY(1.2) rotate(1deg); }
        50% { transform: translateX(-3%) scaleY(0.8) rotate(-1deg); }
        75% { transform: translateX(4%) scaleY(1.1) rotate(0.5deg); }
    }

    /* ── Floating geometric shapes ── */
    .geo-shapes {
        position: fixed;
        inset: 0;
        z-index: 0;
        pointer-events: none;
        overflow: hidden;
    }
    .geo-shape {
        position: absolute;
        border: 1px solid rgba(26,179,148,0.12);
        opacity: 0;
        animation: geoFloat 20s linear infinite;
    }
    .geo-shape.hex {
        width: 60px; height: 60px;
        clip-path: polygon(50% 0%, 100% 25%, 100% 75%, 50% 100%, 0% 75%, 0% 25%);
        background: rgba(26,179,148,0.06);
        border: none;
    }
    .geo-shape.ring {
        width: 80px; height: 80px;
        border-radius: 50%;
        border: 1px solid rgba(26,179,148,0.1);
    }
    .geo-shape.diamond {
        width: 40px; height: 40px;
        background: rgba(26,179,148,0.06);
        border: none;
        clip-path: polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%);
    }
    .geo-shape.cross {
        width: 30px; height: 30px;
        background: none;
        border: none;
    }
    .geo-shape.cross::before, .geo-shape.cross::after {
        content: '';
        position: absolute;
        background: rgba(26,179,148,0.09);
    }
    .geo-shape.cross::before { width: 100%; height: 1px; top: 50%; left: 0; }
    .geo-shape.cross::after { width: 1px; height: 100%; left: 50%; top: 0; }

    .geo-shape:nth-child(1)  { left: 5%;  animation-duration: 22s; animation-delay: 0s; }
    .geo-shape:nth-child(2)  { left: 15%; animation-duration: 26s; animation-delay: -3s; }
    .geo-shape:nth-child(3)  { left: 25%; animation-duration: 20s; animation-delay: -7s; }
    .geo-shape:nth-child(4)  { left: 38%; animation-duration: 28s; animation-delay: -2s; }
    .geo-shape:nth-child(5)  { left: 52%; animation-duration: 24s; animation-delay: -10s; }
    .geo-shape:nth-child(6)  { left: 65%; animation-duration: 21s; animation-delay: -5s; }
    .geo-shape:nth-child(7)  { left: 78%; animation-duration: 25s; animation-delay: -8s; }
    .geo-shape:nth-child(8)  { left: 88%; animation-duration: 23s; animation-delay: -1s; }
    .geo-shape:nth-child(9)  { left: 45%; animation-duration: 27s; animation-delay: -13s; }
    .geo-shape:nth-child(10) { left: 70%; animation-duration: 19s; animation-delay: -6s; }

    @keyframes geoFloat {
        0% {
            transform: translateY(110vh) rotate(0deg) scale(0.5);
            opacity: 0;
        }
        10% { opacity: 0.85; }
        90% { opacity: 0.85; }
        100% {
            transform: translateY(-10vh) rotate(360deg) scale(1.2);
            opacity: 0;
        }
    }

    /* ── Glowing orbs ── */
    .glow-orbs {
        position: fixed;
        inset: 0;
        z-index: 0;
        pointer-events: none;
        overflow: hidden;
    }
    .glow-orb {
        position: absolute;
        border-radius: 50%;
        filter: blur(60px);
        animation: orbPulse 8s ease-in-out infinite, orbWander 25s ease-in-out infinite;
    }
    .glow-orb:nth-child(1) {
        width: 200px; height: 200px;
        background: rgba(26,179,148,0.15);
        top: 20%; right: 15%;
        animation-delay: 0s;
    }
    .glow-orb:nth-child(2) {
        width: 150px; height: 150px;
        background: rgba(125,226,207,0.12);
        bottom: 30%; left: 10%;
        animation-delay: -3s;
    }
    .glow-orb:nth-child(3) {
        width: 120px; height: 120px;
        background: rgba(13,107,90,0.18);
        top: 60%; right: 40%;
        animation-delay: -6s;
    }

    @keyframes orbPulse {
        0%, 100% { opacity: 0.6; transform: scale(1); }
        50% { opacity: 1; transform: scale(1.3); }
    }
    @keyframes orbWander {
        0%, 100% { translate: 0 0; }
        25% { translate: 40px -60px; }
        50% { translate: -30px 40px; }
        75% { translate: 50px 20px; }
    }

    /* ── Scanning grid lines ── */
    .scan-grid {
        position: fixed;
        inset: 0;
        z-index: 0;
        pointer-events: none;
        overflow: hidden;
        opacity: 0.06;
    }
    .scan-line-h {
        position: absolute;
        width: 100%;
        height: 1px;
        background: linear-gradient(90deg, transparent 0%, var(--teal-400) 50%, transparent 100%);
        animation: scanH 8s linear infinite;
    }
    .scan-line-v {
        position: absolute;
        height: 100%;
        width: 1px;
        background: linear-gradient(180deg, transparent 0%, var(--teal-400) 50%, transparent 100%);
        animation: scanV 10s linear infinite;
    }
    @keyframes scanH {
        0% { top: -2px; opacity: 0; }
        5% { opacity: 1; }
        95% { opacity: 1; }
        100% { top: 100%; opacity: 0; }
    }
    @keyframes scanV {
        0% { left: -2px; opacity: 0; }
        5% { opacity: 1; }
        95% { opacity: 1; }
        100% { left: 100%; opacity: 0; }
    }

    /* ── Particle canvas ── */
    #particle-canvas {
        position: fixed;
        inset: 0;
        z-index: 0;
        pointer-events: none;
        opacity: 0.65;
    }

    /* ── Noise grain overlay ── */
    body::after {
        content: '';
        position: fixed;
        inset: 0;
        z-index: 1;
        pointer-events: none;
        opacity: 0.04;
        background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)'/%3E%3C/svg%3E");
    }

    /* ── TopAppBar ── */
    header {
        position: fixed;
        top: 0; left: 0; right: 0;
        z-index: 100;
        padding: 0 2rem;
        height: 64px;
        display: flex;
        align-items: center;
        justify-content: space-between;
        background: linear-gradient(to bottom, rgba(1,26,23,0.92), transparent);
        backdrop-filter: blur(16px);
        border-bottom: 1px solid var(--glass-border);
    }
    .logo-mark {
        display: flex;
        align-items: center;
        gap: 10px;
    }
    .logo-icon {
        width: 36px; height: 36px;
        background: linear-gradient(135deg, var(--teal-600), var(--teal-400));
        border-radius: 10px;
        display: flex; align-items: center; justify-content: center;
        box-shadow: 0 0 20px rgba(26,179,148,0.4);
    }
    .logo-title {
        font-family: 'Clash Display', sans-serif;
        font-weight: 700;
        font-size: 1.35rem;
        letter-spacing: -0.04em;
        color: var(--teal-100);
        text-shadow: 0 0 30px rgba(26,179,148,0.3);
    }
    .logo-badge {
        font-family: 'DM Mono', monospace;
        font-size: 9px;
        letter-spacing: 0.15em;
        color: var(--teal-400);
        text-transform: uppercase;
        background: rgba(26,179,148,0.1);
        border: 1px solid rgba(26,179,148,0.2);
        padding: 2px 8px;
        border-radius: 20px;
    }
    .header-actions { display: flex; gap: 8px; }
    .icon-btn {
        width: 36px; height: 36px;
        border-radius: 10px;
        background: var(--glass-bg);
        border: 1px solid var(--glass-border);
        display: flex; align-items: center; justify-content: center;
        cursor: pointer;
        color: var(--teal-200);
        transition: background 0.2s, box-shadow 0.2s, transform 0.15s;
    }
    .icon-btn:hover {
        background: rgba(26,179,148,0.1);
        box-shadow: 0 0 12px rgba(26,179,148,0.2);
        transform: translateY(-1px);
    }

    main {
        position: relative;
        z-index: 2;
        padding-top: 64px;
    }

    /* ── Hero ── */
    .hero {
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        text-align: center;
        padding: 80px 24px 60px;
        position: relative;
        overflow: hidden;
    }
    .hero-eyebrow {
        font-family: 'DM Mono', monospace;
        font-size: 10px;
        letter-spacing: 0.25em;
        text-transform: uppercase;
        color: var(--teal-400);
        margin-bottom: 24px;
        display: flex;
        align-items: center;
        gap: 10px;
        animation: fadeSlideUp 0.8s ease both 0.1s;
    }
    .hero-eyebrow::before, .hero-eyebrow::after {
        content: '';
        display: block;
        width: 40px; height: 1px;
        background: linear-gradient(to right, transparent, var(--teal-400));
    }
    .hero-eyebrow::after { background: linear-gradient(to left, transparent, var(--teal-400)); }
    .hero-title {
        font-family: 'Clash Display', sans-serif;
        font-weight: 700;
        font-size: clamp(3.5rem, 9vw, 7rem);
        line-height: 0.95;
        letter-spacing: -0.04em;
        color: var(--teal-100);
        margin-bottom: 28px;
        animation: fadeSlideUp 0.8s ease both 0.2s;
    }
    .hero-title .accent {
        display: inline-block;
        background: linear-gradient(135deg, var(--teal-400) 0%, var(--teal-200) 50%, #7de2cf 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        position: relative;
    }
    .hero-title .accent::after {
        content: '';
        position: absolute;
        bottom: 4px; left: 0; right: 0; height: 3px;
        background: linear-gradient(90deg, var(--teal-400), var(--teal-200));
        border-radius: 2px;
        opacity: 0.4;
    }
    .hero-sub {
        font-size: 1.1rem;
        color: rgba(192,243,234,0.85);
        max-width: 480px;
        margin: 0 auto 48px;
        line-height: 1.7;
        animation: fadeSlideUp 0.8s ease both 0.35s;
    }
    .hero-cta-group {
        display: flex; gap: 14px; justify-content: center; flex-wrap: wrap;
        animation: fadeSlideUp 0.8s ease both 0.5s;
    }
    .btn-primary {
        display: inline-flex; align-items: center; gap: 8px;
        padding: 14px 32px;
        background: linear-gradient(135deg, var(--teal-600), var(--teal-400));
        color: #011a17;
        font-family: 'Cabinet Grotesk', sans-serif;
        font-weight: 800;
        font-size: 0.9rem;
        letter-spacing: -0.01em;
        border-radius: 100px;
        text-decoration: none;
        box-shadow: 0 8px 32px rgba(26,179,148,0.35), 0 0 0 1px rgba(26,179,148,0.2);
        transition: transform 0.2s, box-shadow 0.2s;
    }
    .btn-primary:hover {
        transform: translateY(-2px);
        box-shadow: 0 16px 48px rgba(26,179,148,0.45), 0 0 0 1px rgba(26,179,148,0.3);
    }
    .btn-secondary {
        display: inline-flex; align-items: center; gap: 8px;
        padding: 14px 28px;
        background: var(--glass-bg);
        border: 1px solid var(--glass-border);
        color: var(--teal-200);
        font-family: 'Cabinet Grotesk', sans-serif;
        font-weight: 700;
        font-size: 0.9rem;
        border-radius: 100px;
        text-decoration: none;
        backdrop-filter: blur(8px);
        transition: background 0.2s, border-color 0.2s, transform 0.2s;
    }
    .btn-secondary:hover {
        background: rgba(26,179,148,0.08);
        border-color: rgba(26,179,148,0.3);
        transform: translateY(-2px);
    }

    /* Floating data-cards in hero */
    .hero-float-card {
        position: absolute;
        background: rgba(10, 74, 62, 0.55);
        border: 1px solid rgba(26,179,148,0.2);
        backdrop-filter: blur(12px);
        border-radius: 16px;
        padding: 12px 18px;
        display: flex; align-items: center; gap: 10px;
        font-family: 'DM Mono', monospace;
        font-size: 11px;
        color: var(--teal-200);
        box-shadow: 0 8px 32px rgba(0,0,0,0.3);
        animation: floatCard 6s ease-in-out infinite;
        pointer-events: none;
    }
    .hero-float-card .dot {
        width: 8px; height: 8px;
        border-radius: 50%;
        animation: pulseDot 2s ease infinite;
    }
    .hero-float-card:nth-child(1) { top: 22%; left: 8%; animation-delay: 0s; }
    .hero-float-card:nth-child(2) { top: 30%; right: 8%; animation-delay: -2s; }
    .hero-float-card:nth-child(3) { bottom: 25%; left: 12%; animation-delay: -4s; }

    @keyframes floatCard {
        0%, 100% { transform: translateY(0px); }
        50% { transform: translateY(-10px); }
    }
    @keyframes pulseDot {
        0%, 100% { opacity: 1; transform: scale(1); }
        50% { opacity: 0.4; transform: scale(0.8); }
    }
    @keyframes fadeSlideUp {
        from { opacity: 0; transform: translateY(28px); }
        to   { opacity: 1; transform: translateY(0); }
    }

    /* Stats strip */
    .stats-strip {
        display: flex;
        justify-content: center;
        gap: 1px;
        background: rgba(255,255,255,0.05);
        border-top: 1px solid var(--glass-border);
        border-bottom: 1px solid var(--glass-border);
        overflow: hidden;
    }
    .stat-item {
        flex: 1;
        max-width: 200px;
        padding: 24px 20px;
        text-align: center;
        background: var(--glass-bg);
        transition: background 0.25s;
    }
    .stat-item:hover { background: rgba(26,179,148,0.06); }
    .stat-value {
        font-family: 'Clash Display', sans-serif;
        font-size: 2rem;
        font-weight: 700;
        color: var(--teal-200);
        letter-spacing: -0.04em;
        line-height: 1;
    }
    .stat-label {
        font-family: 'DM Mono', monospace;
        font-size: 9px;
        letter-spacing: 0.2em;
        text-transform: uppercase;
        color: rgba(192,243,234,0.7);
        margin-top: 6px;
    }

    /* ── Map section ── */
    .section-eyebrow {
        font-family: 'DM Mono', monospace;
        font-size: 10px;
        letter-spacing: 0.2em;
        text-transform: uppercase;
        color: var(--teal-400);
        margin-bottom: 8px;
    }
    .section-title {
        font-family: 'Clash Display', sans-serif;
        font-size: 2.2rem;
        font-weight: 700;
        color: var(--teal-100);
        letter-spacing: -0.04em;
        line-height: 1.1;
    }
    .filter-pill-group {
        display: flex;
        gap: 8px;
        flex-wrap: wrap;
    }
    .filter-pill {
        padding: 7px 16px;
        border-radius: 100px;
        background: var(--glass-bg);
        border: 1px solid var(--glass-border);
        color: rgba(192,243,234,0.8);
        font-family: 'DM Mono', monospace;
        font-size: 10px;
        letter-spacing: 0.12em;
        text-transform: uppercase;
        cursor: pointer;
        transition: all 0.2s;
    }
    .filter-pill.active, .filter-pill:hover {
        background: rgba(26,179,148,0.12);
        border-color: rgba(26,179,148,0.4);
        color: var(--teal-200);
        box-shadow: 0 0 12px rgba(26,179,148,0.15);
    }

    /* ── Filter form ── */
    .filter-pill-group form {
        display: flex;
        flex-wrap: wrap;
        gap: 8px;
        align-items: flex-end;
    }
    .filter-pill-group .form-group {
        display: flex;
        flex-direction: column;
        gap: 8px;
    }
    .filter-pill-group label {
        font-family: 'DM Mono', monospace;
        font-size: 9px;
        letter-spacing: 0.12em;
        text-transform: uppercase;
        color: rgba(192,243,234,0.45);
        padding-left: 4px;
    }
    .filter-pill-group input[type="date"],
    .filter-pill-group input[type="number"],
    .filter-pill-group input[type="text"] {
        padding: 7px 14px;
        border-radius: 100px;
        background: var(--glass-bg);
        border: 1px solid var(--glass-border);
        color: rgba(192,243,234,0.9);
        font-family: 'DM Mono', monospace;
        font-size: 10px;
        letter-spacing: 0.08em;
        outline: none;
        transition: all 0.2s;
        width: 140px;
        color-scheme: dark;
    }
    .filter-pill-group input[type="number"] {
        width: 90px;
    }
    .filter-pill-group input:focus {
        border-color: rgba(26,179,148,0.4);
        background: rgba(26,179,148,0.06);
        box-shadow: 0 0 12px rgba(26,179,148,0.12);
        color: var(--teal-100);
    }
    .filter-pill-group input::placeholder {
        color: rgba(192,243,234,0.25);
    }
    .filter-pill-group button[type="submit"] {
        padding: 7px 18px;
        border-radius: 100px;
        background: rgba(26,179,148,0.12);
        border: 1px solid rgba(26,179,148,0.4);
        color: var(--teal-200);
        font-family: 'DM Mono', monospace;
        font-size: 10px;
        letter-spacing: 0.12em;
        text-transform: uppercase;
        cursor: pointer;
        transition: all 0.2s;
        align-self: flex-end;
    }
    .filter-pill-group button[type="submit"]:hover {
        background: rgba(26,179,148,0.22);
        box-shadow: 0 0 16px rgba(26,179,148,0.2);
        color: var(--teal-100);
    }

    /* ── Resizable map container ── */
    .map-wrapper {
        position: relative;
        max-width: 900px;
        margin: 0 auto;
        border-radius: 24px;
        overflow: hidden;
        border: 1px solid rgba(26,179,148,0.15);
        box-shadow: 0 0 0 1px rgba(255,255,255,0.04), 0 32px 80px rgba(0,0,0,0.5), 0 0 60px rgba(26,179,148,0.08);
        animation: mapReveal 0.9s cubic-bezier(0.22,1,0.36,1) both 0.3s;
        resize: vertical;
        min-height: 360px;
        max-height: 85vh;
        height: 520px;
    }
    @keyframes mapReveal {
        from { opacity: 0; transform: translateY(40px) scale(0.97); }
        to   { opacity: 1; transform: translateY(0) scale(1); }
    }

    .map-topbar {
        position: absolute;
        top: 16px; left: 16px; right: 16px;
        z-index: 20;
        display: flex;
        justify-content: space-between;
        align-items: center;
        pointer-events: none;
    }
    .map-topbar > * { pointer-events: all; }
    .map-status-badge {
        display: flex;
        align-items: center;
        gap: 8px;
        padding: 8px 14px;
        background: rgba(1,26,23,0.75);
        backdrop-filter: blur(14px);
        border: 1px solid rgba(26,179,148,0.25);
        border-radius: 100px;
        font-family: 'DM Mono', monospace;
        font-size: 10px;
        letter-spacing: 0.12em;
        color: var(--teal-200);
        text-transform: uppercase;
    }
    .live-dot {
        width: 7px; height: 7px;
        border-radius: 50%;
        background: var(--teal-400);
        box-shadow: 0 0 8px var(--teal-400);
        animation: pulseDot 1.5s ease infinite;
    }
    .map-controls {
        display: flex;
        gap: 6px;
    }
    .map-ctrl-btn {
        width: 36px; height: 36px;
        border-radius: 10px;
        background: rgba(1,26,23,0.8);
        border: 1px solid rgba(26,179,148,0.2);
        backdrop-filter: blur(10px);
        display: flex; align-items: center; justify-content: center;
        cursor: pointer;
        color: var(--teal-200);
        font-size: 18px;
        font-weight: 700;
        transition: background 0.2s, transform 0.15s;
        user-select: none;
    }
    .map-ctrl-btn:hover {
        background: rgba(26,179,148,0.15);
        transform: scale(1.05);
    }

    .resize-hint {
        position: absolute;
        bottom: 8px;
        left: 50%;
        transform: translateX(-50%);
        z-index: 30;
        display: flex;
        align-items: center;
        gap: 6px;
        font-family: 'DM Mono', monospace;
        font-size: 9px;
        letter-spacing: 0.12em;
        color: rgba(192,243,234,0.35);
        text-transform: uppercase;
        pointer-events: none;
        transition: opacity 0.3s;
    }
    .resize-hint .grip-icon {
        width: 24px; height: 8px;
        display: flex; flex-direction: column;
        gap: 3px; align-items: center; justify-content: center;
    }
    .resize-hint .grip-icon span {
        display: block;
        width: 20px; height: 2px;
        background: rgba(26,179,148,0.35);
        border-radius: 2px;
    }
    .map-wrapper:hover .resize-hint { opacity: 1; }

    #leaflet-map {
        width: 100%;
        height: 100%;
        z-index: 0;
        background: var(--teal-950);
    }
    .leaflet-pane { z-index: 1 !important; }
    .leaflet-top, .leaflet-bottom { z-index: 5 !important; }
    .leaflet-control-zoom { display: none !important; }
    .leaflet-control-attribution { font-size: 9px !important; opacity: 0.4; background: transparent !important; color: var(--teal-400) !important; }

    /* ── Detail slide-out panel ── */
    .slide-panel {
        transform: translateX(calc(100% + 32px));
        transition: transform 0.55s cubic-bezier(0.22, 1, 0.36, 1), opacity 0.4s ease;
        opacity: 0;
    }
    .slide-panel.open {
        transform: translateX(0);
        opacity: 1;
    }
    #detail-panel {
        position: fixed;
        right: 0; top: 0;
        height: 100%;
        width: 100%;
        max-width: 400px;
        background: var(--teal-950);
        background-image:
            radial-gradient(ellipse at 20% 20%, rgba(26,179,148,0.08) 0%, transparent 60%),
            radial-gradient(ellipse at 80% 80%, rgba(10,74,62,0.3) 0%, transparent 60%);
        z-index: 200;
        border-left: 1px solid rgba(26,179,148,0.12);
        box-shadow: -32px 0 80px rgba(0,0,0,0.6);
        display: flex;
        flex-direction: column;
        overflow-y: auto;
    }
    #detail-panel::-webkit-scrollbar { width: 4px; }
    #detail-panel::-webkit-scrollbar-track { background: transparent; }
    #detail-panel::-webkit-scrollbar-thumb { background: rgba(26,179,148,0.3); border-radius: 2px; }

    .panel-inner { padding: 32px 28px; }
    .panel-eyebrow {
        font-family: 'DM Mono', monospace;
        font-size: 9px;
        letter-spacing: 0.2em;
        text-transform: uppercase;
        color: var(--teal-400);
        margin-bottom: 4px;
    }
    .panel-station-name {
        font-family: 'Clash Display', sans-serif;
        font-size: 1.8rem;
        font-weight: 700;
        color: var(--teal-100);
        letter-spacing: -0.04em;
        line-height: 1.1;
    }
    .panel-close {
        width: 34px; height: 34px;
        border-radius: 10px;
        background: rgba(26,179,148,0.07);
        border: 1px solid rgba(26,179,148,0.15);
        display: flex; align-items: center; justify-content: center;
        cursor: pointer;
        color: var(--teal-200);
        transition: background 0.2s, transform 0.15s;
    }
    .panel-close:hover { background: rgba(244,63,94,0.12); color: #f43f5e; transform: scale(1.05); }

    /* AQI Gauge */
    .aqi-gauge-wrap {
        background: linear-gradient(135deg, rgba(10,74,62,0.4), rgba(5,46,40,0.6));
        border: 1px solid rgba(26,179,148,0.12);
        border-radius: 20px;
        padding: 32px 24px;
        text-align: center;
        margin: 24px 0;
        position: relative;
        overflow: hidden;
    }
    .aqi-gauge-wrap::before {
        content: '';
        position: absolute;
        top: -60px; left: -60px;
        width: 180px; height: 180px;
        background: radial-gradient(circle, rgba(26,179,148,0.1), transparent);
        border-radius: 50%;
    }
    .aqi-value-label {
        font-family: 'Clash Display', sans-serif;
        font-size: 4rem;
        font-weight: 700;
        letter-spacing: -0.05em;
        line-height: 1;
        color: var(--teal-100);
    }
    .aqi-status-label {
        font-family: 'DM Mono', monospace;
        font-size: 9px;
        letter-spacing: 0.2em;
        text-transform: uppercase;
        margin-top: 4px;
        margin-bottom: 16px;
    }
    .aqi-desc {
        font-size: 0.82rem;
        color: rgba(192,243,234,0.8);
        line-height: 1.6;
    }
    .aqi-bar-track {
        height: 6px;
        background: rgba(255,255,255,0.06);
        border-radius: 3px;
        margin: 16px 0 8px;
        overflow: hidden;
    }
    .aqi-bar-fill {
        height: 100%;
        border-radius: 3px;
        transition: width 0.8s cubic-bezier(0.22,1,0.36,1), background 0.5s;
    }

    /* Weather grid */
    .weather-grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 10px;
        margin-bottom: 24px;
    }
    .weather-card {
        background: rgba(10,74,62,0.3);
        border: 1px solid rgba(26,179,148,0.1);
        border-radius: 14px;
        padding: 14px 10px;
        text-align: center;
        transition: background 0.2s, border-color 0.2s;
    }
    .weather-card:hover {
        background: rgba(26,179,148,0.1);
        border-color: rgba(26,179,148,0.25);
    }
    .weather-icon { font-size: 20px; color: var(--teal-400); margin-bottom: 6px; }
    .weather-value { font-family: 'Clash Display', sans-serif; font-size: 1.1rem; font-weight: 700; color: var(--teal-100); }
    .weather-key {
        font-family: 'DM Mono', monospace;
        font-size: 8px;
        letter-spacing: 0.15em;
        text-transform: uppercase;
        color: rgba(192,243,234,0.7);
        margin-top: 2px;
    }

    /* Pollutant rows */
    .section-label {
        font-family: 'DM Mono', monospace;
        font-size: 9px;
        letter-spacing: 0.2em;
        text-transform: uppercase;
        color: rgba(192,243,234,0.7);
        margin-bottom: 12px;
    }
    .pollutant-row {
        background: rgba(10,74,62,0.25);
        border: 1px solid rgba(26,179,148,0.08);
        border-radius: 14px;
        padding: 14px 16px;
        display: flex;
        align-items: center;
        justify-content: space-between;
        margin-bottom: 8px;
        transition: background 0.2s, border-color 0.2s;
    }
    .pollutant-row:hover {
        background: rgba(26,179,148,0.08);
        border-color: rgba(26,179,148,0.2);
    }
    .pollutant-badge {
        width: 34px; height: 34px;
        border-radius: 10px;
        display: flex; align-items: center; justify-content: center;
        font-family: 'DM Mono', monospace;
        font-size: 10px;
        font-weight: 500;
    }
    .pollutant-name { font-family: 'Cabinet Grotesk', sans-serif; font-size: 0.9rem; font-weight: 700; color: var(--teal-100); }
    .pollutant-type { font-family: 'DM Mono', monospace; font-size: 9px; letter-spacing: 0.1em; text-transform: uppercase; color: rgba(192,243,234,0.65); }
    .pollutant-val { font-family: 'Clash Display', sans-serif; font-size: 1.1rem; font-weight: 700; color: var(--teal-100); }
    .pollutant-unit { font-family: 'DM Mono', monospace; font-size: 9px; color: rgba(192,243,234,0.65); }

    .btn-history {
        display: flex; align-items: center; justify-content: center; gap: 8px;
        width: 100%;
        padding: 14px;
        background: linear-gradient(135deg, var(--teal-800), var(--teal-600));
        border: 1px solid rgba(26,179,148,0.25);
        border-radius: 14px;
        color: var(--teal-100);
        font-family: 'Cabinet Grotesk', sans-serif;
        font-weight: 800;
        font-size: 0.88rem;
        cursor: pointer;
        margin-top: 24px;
        transition: transform 0.2s, box-shadow 0.2s, background 0.2s;
        text-decoration: none;
    }
    .btn-history:hover {
        transform: translateY(-1px);
        box-shadow: 0 8px 24px rgba(26,179,148,0.2);
        background: linear-gradient(135deg, var(--teal-600), var(--teal-400));
        color: #011a17;
    }

    /* ── Bottom nav (mobile) ── */
    .bottom-nav {
        display: none;
    }
    @media (max-width: 768px) {
        .bottom-nav {
            display: flex;
            position: fixed;
            bottom: 0; left: 0; right: 0;
            background: rgba(1,26,23,0.9);
            backdrop-filter: blur(20px);
            border-top: 1px solid var(--glass-border);
            padding: 12px 24px 20px;
            justify-content: space-around;
            z-index: 100;
        }
        .bottom-nav-item {
            display: flex; flex-direction: column; align-items: center; gap: 4px;
            cursor: pointer;
        }
        .bottom-nav-icon {
            width: 42px; height: 42px;
            border-radius: 14px;
            display: flex; align-items: center; justify-content: center;
            transition: all 0.2s;
        }
        .bottom-nav-icon.active {
            background: linear-gradient(135deg, var(--teal-600), var(--teal-400));
            box-shadow: 0 4px 16px rgba(26,179,148,0.3);
            color: #011a17;
        }
        .bottom-nav-label {
            font-family: 'DM Mono', monospace;
            font-size: 7px;
            letter-spacing: 0.15em;
            text-transform: uppercase;
        }
    }

    @keyframes marker-pulse {
        0% { transform: scale(1); opacity: 0.5; }
        100% { transform: scale(2.8); opacity: 0; }
    }
    @keyframes scanline {
        0% { transform: translateY(-100%); }
        100% { transform: translateY(100vh); }
    }
</style>
